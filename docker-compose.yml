services:
  chromadb:
    image: chromadb/chroma:latest
    ports:
      - "8000:8000"
    volumes:
      - chroma-data:/chroma/chroma

  app:
    build: .
    ports:
      - "7860:7860"
    env_file:
      - .env
    depends_on:
      - chromadb

volumes:
  chroma-data:
